# Get a key: https://aistudio.google.com/apikey
# Paste it on the line below (no quotes, no spaces). Then save and run: npm run dev
GEMINI_API_KEY=
# Optional — if chat still fails, try another model id (e.g. gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
